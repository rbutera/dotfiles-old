install_extras() {
	# install starship
	# install brew && packages
	# install nodenv
	# install latest node
	# install yarn, pnpm
	# install fzf, fzy, peco
	# install exa, htop
	# give instructions for switching to zsh
}
